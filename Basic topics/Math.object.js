// Mathematical operators 
// Addition op : +
// subtract op : -
// mul       op: *
// div       op:/
// remainder  op:%
// exponentiation op:**  (its use for power  (5**2 =25) (5**3=125))


// Math object

// Math {abs: ƒ, acos: ƒ, acosh: ƒ, asin: ƒ, asinh: ƒ, …}


// Math.sqrt(16)  = 4   (use for square root )

// Math.pow(3*3)  = 27     (its use for power  (5**2 =25) (5**3=125)

// Math.floor(2.4) = 2      (remove all decimal num) (it also small num)  ex= Math.floor(-2.5)=-3  (bz -3 is small)

// Math.ceil(2.4) = 3       (it also big )       ex=Math.ceil(-2.5)=-2    (bz -2 is big)

// Math.round(2.2)=2      Math.round(2.7)=3       (round figure numbers)

// Math.random()=         (provide num min to 0 and closer to 1)  ex= Math.random()*20  (it provide num min to 0 and closer to 20 but not 20)

// if we divide any num from 0 then we get =(infinity)  (infinity is a keyword )  ex=8/0 = infinity

