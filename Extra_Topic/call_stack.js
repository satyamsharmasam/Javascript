// Call Stack(call stack deleted after execution of program)

// definition=> Call stack is a mechanism which is help to javascript engine to track
// which line in running and which function called everything tracked by call stack

// every function has their limit to call if we will cross it then call stack is overflow and
// show an error thats also called stack overflow
