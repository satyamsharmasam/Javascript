// Access siblings of elements or parent and children

// if we want to access parent or child of any element then we will use these

// To access parent
// .parentElement
// EX- in this we will access all parent of a tag until html

// const link = document.querySelector('body > p:nth-child(4) > a:nth-child(2)') //this select by js path
// link.parentElement.parentElement.parentElement
// in this we are access all parent of a tag
// this is nested
// same working for children
//

// To access children
// .children

// Now we will access siblings of elements

// To access next sibling of element
// .nextElementSibling

// To access previous sibling of element
// .previousElementSibling
