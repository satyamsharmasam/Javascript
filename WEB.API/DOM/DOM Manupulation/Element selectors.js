// ** Selecting element  (these all are store in variable and use by var name)
// These are help us to selecting elements and change them by js

// 1. Tag selector

// document.getElementsByTagName('Tag name')  // it provide collect like array name (html collection) in which our tags presented

// 2. class selector

// document.getElementsByClassName('class name')  // it provide all of those element which has this class in form of (html collection) collection like array

// 3. id selector

// document.getElementById('id name')  // it provide one single element bcz id is always be unique

// 4. query selector's  (it fully flexible and working like css selectors )

// document.querySelector('class name ,id name,tag name ,attribute name') // it provide single element by (.) for class (#) for id like css selector

// document.querySelectorAll('class name ,id name,tag name ,attribute name') // it provide all element in the form of (Node list) by (.) and (#) which has same class or Id

// For particular space (easy way to selecting) by this js finding elements in particular space not in full document

// const ul=document.querySelector('ul')
// const inUL=ul.querySelector('take any data which is in ul')
