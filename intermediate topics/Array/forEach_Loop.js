// ******** For each loop (Array Method)

const sam = 10
const days = [
  'Monday',
  'Tuesday',
  'Wednesday',
  'Thursday',
  'Friday',
  'Saturday',
  'Sunday',
]

days.forEach((day, index) => {
  console.log(index, day)
})

// We don't able to return any value from it , it always return undefined
