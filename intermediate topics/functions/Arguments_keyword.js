// Argument Keyword (it is not wark in arrow function)

// Ex-

// function add() {
// console.log(arguments)     // Argument Keyword
// }

// Argument Keyword ( is show all values in form of object array
//  which we will pass in function call even if function has only
// two parameter but it show all values what we passed)
// its like array but not array because we will not perform any array methods

// We also excess those values by looping or index

// function that take unlimited values as input and output their sum(even without
//  expecting any parameter only by the help of argument keyword)

// function add() {
//   let sum = 0
//   for (let i = 0; i < arguments.length; i++) {
//     // console.log(arguments[i])
//     sum += arguments[i]
//   }
//   return sum
// }
